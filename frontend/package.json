{
  "name": "local-ai-assistant-frontend",
  "private": true,
  "version": "1.0.0",
  "description": "Local AI Assistant — React frontend, compiled to frontend/dist for the Windows laptop.",
  "scripts": {
    "build": "node build/build.mjs"
  },
  "dependencies": {
    "react": "^18",
    "react-dom": "^18"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
